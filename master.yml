- hosts: masters[0]
  become: yes
  tasks:
#    - name: initialize the cluster
#      shell: kubeadm init --control-plane-endpoint "'{{  masters[0]  }}':6443" --upload-certs


- hosts: masters
  become: yes
  tasks:
    - name: create .kube directory
      file:
        path: $HOME/.kube
        state: directory
        mode: 0775 
  
    - name: copy admin.conf to user's kube config
      copy:
        src: /etc/kubernetes/admin.conf
        dest: $HOME/.kube/config
        remote_src: yes



